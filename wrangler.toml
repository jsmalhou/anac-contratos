name = "anac-contratos"
main = "api/worker.ts"
compatibility_date = "2025-06-19"
compatibility_flags = ["nodejs_compat"]

# Assets (frontend)
[site]
bucket = "./dist/public"

# Build command - only builds frontend, wrangler bundles the worker
[build]
command = "npm run build:frontend"
